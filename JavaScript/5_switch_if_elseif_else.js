switch (cor) {
    case "Red":
      // Código a ser executado se a expressão for igual a valor1
      
      break;
    case valor2:
      // Código a ser executado se a expressão for igual a valor2
      break;
    // Mais casos podem ser adicionados conforme necessário
    default:
      // Código a ser executado se a expressão não corresponder a nenhum caso
  }

let opcao = onClick

  switch (opcao) {
    case "Adicao":
      // Código a ser executado se a expressão for igual a valor1
      
      break;
    case "Subtracao":
      // Código a ser executado se a expressão for igual a valor2
      break;
    // Mais casos podem ser adicionados conforme necessário
    default:
      // Código a ser executado se a expressão não corresponder a nenhum caso
  }
}

  cor

  "EstadoImprimir" -> Vermelho
  "EstadoTratar" -> Azul
  "EstadoReceber" -> Verde

if (cor === "EstadoImprimir") {
  console.log("Vermelho")
  } else if (expressao === "EstadoTratar") {
    console.log("Azul")
  } else if (expressao === "EstadoReceber") {
    console.log("Verde")
  }
  else {
    console.log("Erro")
  }